<%- include ('header') %>
<% if(locals.post) { %>
  <form action="/updatePost?id=<%= post.idpost %>" method="post" style="width: 50%; margin: auto;">
      <div class="form-group">
        <h2>MODIFIER LE POST N° <%= post.idpost %> </h2>
        <label for="Titre">Titre</label>
        <input type="text" class="form-control" id="Titre"  value="<%= post.titre %>" name="titre">
        <small id="emailHelp" class="form-text text-muted">ici mettez un ttitre a votre post.</small>
      </div>
      <div class="form-group">
        <label for="Description">Description</label>
        <input type="text" class="form-control" id="Description" value="<%= post.description %>" name="description" style="height: 200px;"></textarea>
      </div>
      <div class="form-group">
        <label for="Titre">Fichier</label>
        <% if(post.postimage) { %>
          <img class="card-img-top" src="images/<%= post.postimage %>" alt="Card image cap">
        <% } %>
        <input type="file" class="form-control" id="Titre" name="avatar">
        <small id="" class="form-text text-muted">ici mettez une image a votre post.</small>
      </div>
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>

<% } %>