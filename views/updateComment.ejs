<%- include ('header') %>
<% if(comment) { %>
  <form action="/update-comment?idcomment=<%= comment.idcomment %>" method="post" style="width: 50%; margin: auto;">
      <div class="form-group">
        <h2>MODIFIER LE POST N° <%= comment.idcomment %> </h2>
      </div>
      <div class="form-group">
        <input type="text" class="form-control" id="Titre" name="comment" value="<%= comment.comment %>">
      </div>
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>

<% } %>